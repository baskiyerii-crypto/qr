# Yerel production testi için (Coolify'da gerekmez):
#   docker compose -f docker-compose.prod.yml -f docker-compose.prod.local.yml --env-file .env.production up -d --build

services:
  web:
    ports:
      - '8080:80'
